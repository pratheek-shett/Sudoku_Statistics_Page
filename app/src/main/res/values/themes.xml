<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Sudoku_Statistics_Page" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@drawable/gradient_color</item>
        <item name="android:background">@drawable/gradient_color</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/gradient_color</item>
    </style>

    <style name="Theme.Sudoku_Statistics_Page" parent="Base.Theme.Sudoku_Statistics_Page">
        <item name="android:windowActionBar">false</item> <!-- Ensures no action bar -->
    </style>
</resources>
